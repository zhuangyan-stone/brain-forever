# ============================================================
# BrainForever remote-server 配置文件
# 优先级: --config CLI 参数 > CONFIG_PATH 环境变量 > 本文件
# ============================================================

[server]
addr = ":9090"

[logger]
file = "log/remote-server.log"
level = "INFO"
